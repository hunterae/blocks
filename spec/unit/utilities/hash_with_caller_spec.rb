require 'spec_helper'

describe Blocks::HashWithCaller do
  context 'Initialization' do
    xit 'should instantiate a hash of callers'
    xit 'call #add_options with the first argument and the extracted options'
    xit 'should call super without a block, even when one is passed in'
  end

  context '#to_s' do
    xit "todo"
  end

  context '#add_options' do
    xit "todo"
  end
end